---
import { url } from '../lib/url';
import Layout from '../layouts/Layout.astro';
import vocabulary from '../data/vocabulary.json';
---

<Layout title="About" description="About the ONGA ontology">
  <article class="prose prose-gray max-w-none">
    <h1>About ONGA</h1>

    <p class="lead text-xl text-gray-600">
      {vocabulary.description}
    </p>

    <h2>The Problem</h2>
    <p>
      When you have a genomic data file (like a BED file), what does it actually contain?
      Is it peaks from ChIP-seq? ATAC-seq? IDR-filtered peaks? Methylation states? Signal tracks?
    </p>
    <p>
      The ENCODE consortium developed approximately 310 internal terms for describing file
      contents ("output types"), but these terms aren't formalized as an ontology, lack definitions,
      and aren't easily usable outside ENCODE.
    </p>
    <p>
      Existing ontologies like <a href="https://edamontology.org/">EDAM</a> cover broad
      bioinformatics concepts but lack the granularity needed for real-world genomic file annotation.
    </p>

    <h2>The Solution</h2>
    <p>
      ONGA formalizes this vocabulary with:
    </p>
    <ul>
      <li><strong>Hierarchical organization</strong> — {vocabulary.stats.totalCategories} categories organizing {vocabulary.stats.totalTerms} specific terms</li>
      <li><strong>Proper definitions</strong> — Each term has a description explaining its meaning</li>
      <li><strong>Ontology mappings</strong> — {vocabulary.stats.totalMappings} terms mapped to EDAM, and {vocabulary.stats.elementTypeCoverage} terms whose rows are typed against the Sequence Ontology via <code>onga:has_element_type</code></li>
      <li><strong>Clear provenance</strong> — Terms track their source (ENCODE, UCSC, etc.)</li>
      <li><strong>Extension mechanism</strong> — New terms can be proposed and reviewed</li>
    </ul>

    <h2>Scope</h2>

    <h3>ONGA and SO: sets vs. elements</h3>
    <p>
      An ONGA term names a <strong>set</strong> of genomic elements — a file, a track, a set
      of rows. A Sequence Ontology class names an <strong>individual element type</strong>.
      <code>onga:enhancers</code> is "a file whose rows are instances of <code>SO:0000165
      enhancer</code>", not "an enhancer", and a set of enhancers is not an enhancer.
    </p>
    <p>
      So ONGA never claims to be equivalent to an SO class. Where every row of a file
      instantiates one SO class, ONGA records that with its own property
      <code>onga:has_element_type</code>. <strong>ONGA is a user of SO, not a competitor to
      it</strong> — SO supplies the element vocabulary, ONGA supplies the set vocabulary and
      the descriptor schemas around it. See <a href={url("/scope")}>the scope page</a> for
      the worked cases.
    </p>

    <h3>In Scope</h3>
    <ul>
      <li><strong>File content types</strong> — Peaks, signals, quantifications, matrices, references</li>
      <li><strong>Track descriptor schemas</strong> — Format, interpretation, provenance, geometry, and reference genome (Layer 2)</li>
      <li><strong>File records</strong> — <code>GenomicAnnotationFile</code>, <code>File</code>, and their GA4GH-DRS-shaped components: access methods, checksums, input sources, quality assessments (Layer 3)</li>
      <li><strong>Investigation records</strong> — Experiments, studies, analyses, samples (with the <code>BiospecimenClassification</code> vocabulary), donors, deposits, and metadata deposit documents (Layer 4), ported from the FGA-WG schema with machine-readable lineage</li>
    </ul>

    <h3>Delegated to existing ontologies</h3>
    <p>
      ONGA never mints its own terms for these axes. Record and investigation slots carry
      them as data-level ontology references (CURIE + label), and the content vocabularies
      keep them factored out entirely:
    </p>
    <ul>
      <li><strong>Assay types</strong> → OBI/EFO (e.g. <code>Experiment.assay_type</code>)</li>
      <li><strong>Organisms</strong> → NCBITaxon (e.g. <code>Donor.species_taxon</code>)</li>
      <li><strong>Cell types</strong> → Cell Ontology (CL) (e.g. <code>Sample.cell_type</code>)</li>
      <li><strong>Anatomical terms</strong> → UBERON (e.g. <code>Sample.organism_tissue</code>)</li>
      <li><strong>Sample sex</strong> → PATO (via <code>Donor.donor_sex</code> — distinct from the reference build's <code>build_sex</code>)</li>
      <li><strong>Individual sequence feature types</strong> → <a href="http://www.sequenceontology.org/">Sequence Ontology (SO)</a>. ONGA terms name <em>sets</em> of features; SO names the elements inside them. See <a href={url("/scope")}>the scope page</a>.</li>
    </ul>

    <h2>Technical Details</h2>
    <dl class="grid grid-cols-2 gap-4 not-prose text-sm bg-gray-50 rounded-lg p-6">
      <div>
        <dt class="text-gray-500">Version</dt>
        <dd class="font-medium">{vocabulary.version}</dd>
      </div>
      <div>
        <dt class="text-gray-500">License</dt>
        <dd class="font-medium">
          <a href={vocabulary.license} class="text-blue-600 hover:underline">CC BY 4.0</a>
        </dd>
      </div>
      <div>
        <dt class="text-gray-500">Namespace</dt>
        <dd class="font-mono text-xs">{vocabulary.prefix}</dd>
      </div>
      <div>
        <dt class="text-gray-500">Format</dt>
        <dd class="font-medium">LinkML</dd>
      </div>
      <div>
        <dt class="text-gray-500">Created by</dt>
        <dd class="font-medium">{vocabulary.createdBy}</dd>
      </div>
      <div>
        <dt class="text-gray-500">Created</dt>
        <dd class="font-medium">{vocabulary.creationDate}</dd>
      </div>
    </dl>

    <h2>Links</h2>
    <ul>
      <li><a href="https://github.com/databio/onga">GitHub repository</a></li>
      <li><a href={url("/schema")}>Schema browser</a> — Every class and enum, by layer</li>
      <li><a href={url("/api")}>Schema Registry API</a> — The schema as JSON Schema, served as a static GA4GH Schema Registry</li>
      <li><a href="https://www.encodeproject.org/">ENCODE Project</a> — Source of output_type vocabulary</li>
      <li><a href="https://edamontology.org/">EDAM Ontology</a> — Target for upstream contribution</li>
      <li><a href="https://linkml.io/">LinkML</a> — Schema language used</li>
      <li><a href="https://mapping-commons.github.io/sssom/">SSSOM</a> — Mapping standard used</li>
    </ul>

    <h2>Contact</h2>
    <p>
      For questions, suggestions, or to propose new terms, please
      <a href="https://github.com/databio/onga/issues">open an issue</a> on GitHub.
    </p>
  </article>
</Layout>
