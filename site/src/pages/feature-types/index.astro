---
import { url } from '../../lib/url';
import Layout from '../../layouts/Layout.astro';
import featureTypes from '../../data/feature-types.json';
import featureTypesByCategory from '../../data/feature-types-by-category.json';

const categories = Object.keys(featureTypesByCategory).sort();
---

<Layout title="FeatureTypes" description="Biological feature types - WHAT biology it represents">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900 mb-2">FeatureType</h1>
    <p class="text-green-600 font-medium">{featureTypes.length} terms</p>
    <p class="text-gray-600 mt-2">
      Biological feature types — the biological entity or concept the data represents.
      Describes <em>WHAT</em> biological phenomenon the features capture.
    </p>
  </div>

  <!-- Search -->
  <div class="bg-white rounded-lg p-4 shadow-sm mb-8">
    <input
      type="search"
      id="search"
      placeholder="Filter FeatureTypes..."
      class="w-full px-4 py-2 border border-gray-300 rounded focus:ring-2 focus:ring-green-500"
    />
  </div>

  <!-- By Category -->
  <div class="space-y-8">
    {categories.map(category => (
      <section id={category}>
        <h2 class="text-xl font-semibold text-gray-900 mb-4 capitalize">
          {category.replace(/_/g, ' ')}
          <span class="text-sm font-normal text-gray-500 ml-2">
            ({featureTypesByCategory[category].length})
          </span>
        </h2>
        <div class="bg-white rounded-lg shadow-sm divide-y divide-gray-100">
          {featureTypesByCategory[category].map((term: any) => (
            <a
              href={url(`/feature-types/${term.slug}`)}
              class="term-item block p-4 hover:bg-gray-50"
              data-name={term.name.toLowerCase()}
              data-description={term.description.toLowerCase()}
            >
              <div class="flex items-start justify-between">
                <div>
                  <h3 class="font-medium text-gray-900">{term.name}</h3>
                  <p class="text-sm text-gray-500 mt-1">{term.description}</p>
                  {term.seeAlso?.length > 0 && (
                    <p class="text-xs text-blue-600 mt-2">
                      ← {term.seeAlso.join(', ')}
                    </p>
                  )}
                </div>
                {term.edamMapping && (
                  <span class="text-xs bg-green-100 text-green-800 px-2 py-1 rounded shrink-0 ml-4">
                    EDAM
                  </span>
                )}
              </div>
            </a>
          ))}
        </div>
      </section>
    ))}
  </div>

  <script>
    const searchInput = document.getElementById('search') as HTMLInputElement;
    const items = document.querySelectorAll('.term-item') as NodeListOf<HTMLElement>;

    searchInput?.addEventListener('input', (e) => {
      const query = (e.target as HTMLInputElement).value.toLowerCase().trim();
      items.forEach(item => {
        const name = item.dataset.name || '';
        const desc = item.dataset.description || '';
        const matches = !query || name.includes(query) || desc.includes(query);
        item.classList.toggle('hidden', !matches);
      });
    });
  </script>
</Layout>
